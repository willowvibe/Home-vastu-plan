services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped

  # Uncomment to run the collaboration server alongside the frontend
  # server:
  #   build:
  #     context: .
  #     dockerfile: server/Dockerfile
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     - PORT=4000
  #     - GEMINI_API_KEY=${GEMINI_API_KEY}
  #   restart: unless-stopped
